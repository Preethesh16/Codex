# Copy this file to server/.env and fill in real values.
# DO NOT COMMIT server/.env

OPENAI_API_KEY=your-openai-api-key
CODEX_MODEL=gpt-5.6-sol
OPENAI_TRANSCRIBE_MODEL=gpt-4o-transcribe
STARTUPFORGE_TOKEN_ENCRYPTION_KEY=replace-with-a-long-random-secret
STARTUPFORGE_SERVICE_TOKEN=replace-with-the-same-long-random-secret-used-by-orbit
PORT=3001
STARTUPFORGE_DB_PATH=startupforge.db
VERCEL_TOKEN=                              # From vercel.com/account/tokens (optional)
GENERATED_MVPS_PATH=../generated-mvps
# Linux uses Bubblewrap automatically for generated-project commands. On other
# hosts, leave this false unless you explicitly accept unsandboxed local builds.
STARTUPFORGE_ALLOW_UNSANDBOXED_BUILDS=false
CLIENT_URL=http://localhost:5173

# Fix Center — path to a local Google Forms/Sheets CSV export.
# Binary spreadsheets stay local and are not parsed by the demo privacy gate.
FEEDBACK_CSV_PATH=feedback.csv

# GitHub source publishing uses this machine's SSH key. The target repository
# must already exist under this owner; repository creation and deployment are
# intentionally separate actions.
GITHUB_SSH_OWNER=Preethesh16
